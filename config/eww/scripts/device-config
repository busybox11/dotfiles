#!/bin/bash

CPU_TEMP_ID="K10TEMP_TCTL"

# Check for work laptop (dell_ddv-virtual-0 indicates Dell)
if sensors | grep "dell_ddv-virtual-0" > /dev/null; then
    CPU_TEMP_ID="CORETEMP_PACKAGE_ID_0"
fi

# Output JSON (no file writing)
json="{\"CPU_TEMP_ID\": \"$CPU_TEMP_ID\"}"
echo "$json"
